# 2026-04-11 수정 작업 기록

오늘 진행된 Yocto 프로젝트 수정 사항 및 QEMU 실행 환경 관련 패치 내역입니다.

## 1. 전용 실행 스크립트 추가
- **파일명**: `scripts/qemu.sh`
- **내용**: 
    - `KAS_BUILD_DIR` 환경 변수를 `output/qemuarm64/build`로 자동 설정.
    - `kas shell`을 통해 `runqemu nographic slirp`를 일관되게 실행할 수 있도록 함.
    - 실행 권한(`+x`) 부여 완료.

## 2. 가이드 문서(README) 경로 최적화
- **대상**: 
    - `manifest/docs/qemuarm64/README.md`
    - `manifest/docs/rpi3/README.md`
- **수정 사항**:
    - 기존의 가상 경로(`/home/yang/work/yocto/ai-agent_test0/...`)를 현재 실제 폴더 구조인 `/home/yang/work/yocto/test-20260409/manifest` 및 `output/`으로 동기화.
    - 복잡한 `kas` 명령어 대신 `./scripts/qemu.sh`를 사용하는 간단한 가이드로 업데이트.

## 3. QEMU 로그인 문제 해결 (Password Skip)
- **대상**: `manifest/kas/qemuarm64/kas-qemuarm64.yml`
- **변경 사항**: 
    - `EXTRA_IMAGE_FEATURES += "debug-tweaks"` 옵션 추가.
- **결과**: `root` 계정 로그인 시 비밀번호 없이 바로 쉘 진입이 가능해짐 (재빌드 후 확인 완료).

## 4. 프로세스 충돌 해결
- 이미지 파일 점유(`write lock`) 문제 발생 시 기존 `qemu-system-aarch64` 프로세스를 찾아 정리하는 방법 안내 및 조치.

---
**기록 일시**: 2026년 4월 11일 22:13
**작업자**: Antigravity (AI Coding Assistant)
